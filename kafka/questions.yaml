labels:
  io.cattle.role: project # options are cluster/project
  io.rancher.app_min_version: 11.2.2
questions:
# Private Registry
- variable: global.imageRegistryEnable
  default: false
  type: boolean
  label: Private Registry
  show_subquestion_if: true
  group: "Global Settings"
  subquestions:
  - variable: global.imageRegistry
    type: string
    label: Private Registry path
# Kafka Configurations
- variable: replicaCount
  default: 3
  description: "Replicas of Kafka Brokers"
  type: int
  min: 1
  max: 8
  required: true
  label: Kafka Brokers
  group: "Kafka Settings"
- variable: logRetentionCheckIntervalMs
  default: 300000
  description: "The maximum amount of time a message can sit in a log before we force a flush."
  type: int
  min: 0
  max: 10000000
  required: true
  label: Message Flush Time (ms)
  group: "Kafka Settings"
# Zookeeper Configurations
- variable: zookeeper.replicaCount
  default: 3
  description: "Replicas of Zookeeper"
  type: int
  min: 1
  max: 8
  required: true
  label: Zookeeper replica
  group: "Zookeeper Settings"